// Video Simple Mode: Empty Generator for Static Leaf Nodes
// Since the prompt string is directly embedded in the category definition (background.js),
// we just need to return an empty array to satisfy the builder logic that looks for a generator.

export const empty_generator = [];
